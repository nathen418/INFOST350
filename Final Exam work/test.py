# validate the user typed a correct template name
# # if not, ask the user to re-enter the template name
# if the user typed a correct template name, then open the template
# Print the template name, type, body, and variables
# Ask the user to enter the values for the variables
# Print the body with the variables filled in
# Ask the user if they want to send the email
